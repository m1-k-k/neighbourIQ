# TomTom Traffic API key — free tier at https://developer.tomtom.com/
# Required only for live traffic data at /dashboard and /resident. Without it, the app still
# works (flood/crime/weather stay real) but traffic readings show as unconfigured.
TOMTOM_API_KEY=
